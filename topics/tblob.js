"use strict";

var step = require("step");
var h = require("whispeerHelper");

var streamAPI = {
	pushBlob: function (data, fn, view) {
		debugger;
		console.log("yeha!");
	},
	getBlob: function (data, fn, view) {
		console.log("yeha!");
	}
};

module.exports = streamAPI;